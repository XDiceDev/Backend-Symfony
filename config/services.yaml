# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        public: false

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/*'
        exclude: '../src/{DependencyInjection,Entity,Migrations,Tests,Kernel.php}'

    app.admin.cottage:
        class: App\Admin\CottageAdmin
        arguments: [~, 'App\Entity\Cottage', ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: content, label: 'Коттеджи' }

    app.admin.amenity:
        class: App\Admin\AmenityAdmin
        arguments: [~, 'App\Entity\Amenity', ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: content, label: 'Удобства' }

    app.admin.booking:
        class: App\Admin\BookingAdmin
        arguments: [~, 'App\Entity\Booking', ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: bookings, label: 'Бронирования' }

    app.admin.client_contact:
        class: App\Admin\ClientContactAdmin
        arguments: [~, 'App\Entity\ClientContact', ~]
        tags:
            - { name: sonata.admin, manager_type: orm, group: contacts, label: 'Контакты клиентов' }

    app.admin.user:
        class: App\Admin\UserAdmin
        arguments: [~, 'App\Entity\User', ~, '@security.password_hasher']
        tags:
            - { name: sonata.admin, manager_type: orm, group: users, label: 'Пользователи' }

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones